import React, { ReactElement } from "react";
import './main.css';

const Main = (): ReactElement => {
    return(
        <section className="main">
            Main Page!
        </section>
    );
};

export default Main;